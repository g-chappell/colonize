{
  "name": "@colonize/mobile",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --workspace=@colonize/web && cap sync",
    "sync": "cap sync",
    "open:ios": "cap open ios",
    "open:android": "cap open android",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
